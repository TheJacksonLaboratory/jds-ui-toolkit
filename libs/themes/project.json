{
  "name": "themes",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "lib/themes/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": [],
  "targets": {
     "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/libs/themes"],
      "options": {
        "project": "libs/themes/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/themes/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/themes/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/themes/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "generate-utilities": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx libs/themes/scripts/generate-utilities.ts"
      }
    },
    "build-with-utilities": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "commands": [
          "nx build themes",
          "npx tsx libs/themes/scripts/generate-utilities.ts"
        ],
        "parallel": false
      }
    }
  }
}
